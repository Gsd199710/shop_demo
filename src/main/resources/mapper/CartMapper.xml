<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace属性：指定当前文件与哪个接口进行映射，需要指定的完整接口路径-->
<mapper namespace="com.demo.shop.mapper.CartMapper">
    <resultMap id="CartEntityMap" type="com.demo.shop.entity.Cart">
        <id column="cid" property="cid"/>
        <result column="created_user" property="createdUser"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_user" property="modifiedUser"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="cid">
        INSERT INTO t_cart(
            uid,pid,price,num,created_user,created_time,
            modified_user,modified_time
        )VALUES (
                    #{uid},#{pid},#{price},#{num},#{createdUser},#{createdTime},
                    #{modifiedUser},#{modifiedTime}
                )
    </insert>
    <update id="updateCartByCid">
        UPDATE t_cart
        SET num = #{num},
            modified_user = #{modifiedUser},
            modified_time = #{modifiedTime}
        WHERE cid = #{cid}
    </update>
    <select id="findCartByPid" resultMap="CartEntityMap">
        SELECT * FROM t_cart
        WHERE uid = #{uid} AND pid = #{pid}
    </select>
    <select id="findVOByUid" resultType="com.demo.shop.vo.CartVo">
        select cid,uid,pid,
               t_cart.price,t_cart.num,
               t_product.title,t_product.image,t_product.price as realprice
        from t_cart left join t_product on t_cart.pid = t_product.id
        where uid = #{uid}
        order by t_cart.modified_time DESC
    </select>
    <select id="findByCid" resultMap="CartEntityMap">
        select * from t_cart where cid = #{cid}
    </select>
    <select id="findVOByCid" resultType="com.demo.shop.vo.CartVo">
        select cid,uid,pid,
               t_cart.price,t_cart.num,
               t_product.title,t_product.image,t_product.price as realprice
        from t_cart left join t_product on t_cart.pid = t_product.id
        where cid in (
                <foreach collection="array" item="cid" separator=",">
                    #{cid}
                </foreach>
            )
        order by t_cart.modified_time DESC
    </select>
</mapper>